"use client";

import { BoardierCanvas } from '../../../../ui/BoardierCanvas';

export default function DemoPage() {
  return (
    <main className="w-screen h-screen">
      <BoardierCanvas />
    </main>
  );
}
